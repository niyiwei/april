spring:
  application:
    name: "april-user-server"

  profiles:
    active: "local"

  cloud:
    nacos:
      discovery:
        server-addr: "http://localhost:8848"
      config:
        server-addr: "http://localhost:8848"
        file-extension: "yml"